# ESDiff
A few-shot color image inpainting framework based on score-based diffusion models. ESDiff utilizes a virtual-mask encoding strategy to construct high-dimensional priors from limited data and integrates low-rank constraints for robust reconstruction
